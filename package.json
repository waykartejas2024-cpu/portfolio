{
  "name": "project-otp-server",
  "version": "1.0.0",
  "description": "Simple demo server to send/verify OTPs (Twilio optional)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "twilio": "^4.0.0"
  }
}
